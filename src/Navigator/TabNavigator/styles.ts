import {StyleSheet} from 'react-native';
import {black, noghreiSilver} from '../../Theme/color';

export default StyleSheet.create({
  label: {color: noghreiSilver, fontWeight: '500', fontSize: 11},
  tabBG: {backgroundColor: black, borderTopWidth: 0},
  headerTitle: {fontSize: 30, fontWeight: '500'},
});
