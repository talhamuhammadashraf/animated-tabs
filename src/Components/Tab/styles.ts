import {StyleSheet} from 'react-native';
import {black} from '../../Theme/color';

export default StyleSheet.create({
  tabBar: {maxHeight: 57, backgroundColor: black},
  tabItem: {
    alignSelf: 'center',
    padding: 16,
  },
});
