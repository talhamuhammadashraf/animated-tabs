import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  image: {
    resizeMode: 'contain',
    marginLeft: 12,
  },
  container: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    alignItems: 'center',
    marginRight: 20,
  },
});
